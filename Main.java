import java.util.Scanner;

public class Main {
    public static void main(String[] args){
        palindromeNumber p1 = new palindromeNumber();
        p1.run();

        twoSum ts1 = new twoSum();
        ts1.run();

        // Creating objects, and then running them.
        // First implementation style is to simply
        // create objects, and then run them one-by-one.

        // The next step would be to create an interface that
        // selects the object to be created.
    }
    // Driver function for the other leetcode classes.
}
